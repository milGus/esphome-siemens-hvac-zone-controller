import esphome.codegen as cg
import esphome.config_validation as cv
from esphome.components import cv as cv_local

CODEOWNERS = ["@milGus"]

# This tells ESPHome to load the accompanying .yaml file from this directory
CONFIG_SCHEMA = cv.Schema({})

async def to_code(config):
    pass
