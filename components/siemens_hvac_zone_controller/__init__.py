import esphome.config_validation as cv

CODEOWNERS = ["@milGus"]

# This is a minimal valid configuration schema for a pure YAML component package
CONFIG_SCHEMA = cv.Schema({})

async def to_code(config):
    pass
